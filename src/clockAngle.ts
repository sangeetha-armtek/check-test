//There can be two angles between hands; we need to print a minimum of two.
// Also, we need to print the floor of the final result angle. For example, if the final angle is 10.61, we need to print 10.
export function angleBetweenMinuteAndHourHand(
  hour: number,
  minute: number
): number {}
